<Window x:Class="_09.CommandExample.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_09.CommandExample"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <StackPanel Margin="5">
        <Button Height="40" Content="Clear Text" Command="{Binding ClearCommand }"/>
        <TextBox Height="150" Text="{Binding TextContent,UpdateSourceTrigger=PropertyChanged}"  />
    </StackPanel>
</Window>
