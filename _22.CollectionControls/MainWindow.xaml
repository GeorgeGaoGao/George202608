<Window x:Class="_22.CollectionControls.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_22.CollectionControls"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="450" Width="800">
    <!--<Window.DataContext>
        <local:MainWindow/>
    </Window.DataContext>-->
  
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" Margin="5" Name="myListBox" DisplayMemberPath="Age" SelectedValuePath="Address"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button  Content="显示结果" Width="100"  Height="30" FontSize="20" Margin="5" Click="Button_Click_1"/>
            <TextBlock Name="myTextblock" Text="{Binding TextToShow}" FontSize="16" VerticalAlignment="Center" Background="LightCoral" />
        </StackPanel>
        
    </Grid>
</Window>
