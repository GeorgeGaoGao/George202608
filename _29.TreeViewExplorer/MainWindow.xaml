<Window x:Class="_29.TreeViewExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_29.TreeViewExplorer"
        mc:Ignorable="d" FontSize="20"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBlock Width="80" Text="Root" VerticalAlignment="Center" FontSize="25"/>
            <TextBox Width="400" VerticalContentAlignment="Center" FontSize="25" Text="{Binding RootFolder}"/>
            <Button Content="Choose Root" Width="130" FontSize="16" Command="{Binding ChooseRootFolderCommand}"/>

        </StackPanel>
        <TreeView Grid.Row="1" ItemsSource="{Binding TreeViewItems}"/>
    </Grid>
</Window>
