<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:_40.ControlTemplateDataTemplateExercise">
    <ControlTemplate TargetType="ListBox" x:Key="CustomListBoxTemplate">
        <Border BorderBrush="Red" BorderThickness="1" Background="LightPink" CornerRadius="10">
            <ScrollViewer>
                <ItemsPresenter/>
            </ScrollViewer>
        </Border>
    </ControlTemplate>
    <DataTemplate DataType="{x:Type local:Person}" x:Key="PersonTemplate">
        <Border BorderBrush="Gray" BorderThickness="1" Margin="2" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Width="100" Text="{Binding Name}"/>
                <TextBlock Width="100" Text="{Binding Age}"/>
                <TextBlock Width="100" Text="{Binding Avatar}"/>
            </StackPanel>
        </Border>
    </DataTemplate>
</ResourceDictionary>