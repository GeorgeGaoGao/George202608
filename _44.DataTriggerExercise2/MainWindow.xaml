<Window x:Class="_44.DataTriggerExercise2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_44.DataTriggerExercise2"
        mc:Ignorable="d" FontSize="20"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DataGrid Grid.Column="0" ItemsSource="{Binding People}" SelectedItem="{Binding SelectedPerson}"
                  AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="姓名" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="年龄" Binding="{Binding Age}"/>
                <DataGridTextColumn Header="地址" Binding="{Binding Address}"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Age}" Value="18">
                            <Setter Property="Background" Value="Green"/>
                            <Setter Property="Foreground" Value="White"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=myCheckBox,Path=IsChecked}" Value="True">
                            <Setter Property="Foreground" Value="Red"/>
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Name}" Value="George"/>
                                <Condition Binding="{Binding Age}" Value="52"/>
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter Property="Background" Value="Red"/>
                                <Setter Property="Height" Value="50"/>
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
        <StackPanel VerticalAlignment="Center" Grid.Column="1" Background="LightBlue" Margin="5">
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="姓名 ">
                    <Run Text="{Binding SelectedPerson.Name}" Foreground="DarkGreen"/>
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="年龄 ">
                    <Run Text="{Binding SelectedPerson.Age}" Foreground="DarkGreen"/>
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="住址 ">
                    <Run Text="{Binding SelectedPerson.Address}" Foreground="DarkGreen"/>
                </TextBlock>
            </StackPanel>
            <CheckBox Content="设置为红色字符" Name="myCheckBox"/>
            <Button Content="增加人员" Width="200" Height="40" Margin="10" Command="{Binding AddPersonCommand}"/>
        </StackPanel>
    </Grid>
</Window>
