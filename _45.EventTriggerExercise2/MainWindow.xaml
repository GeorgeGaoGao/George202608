<Window x:Class="_45.EventTriggerExercise2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_45.EventTriggerExercise2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Storyboard x:Key="OnCheckedStoryboard">
            <DoubleAnimation Storyboard.Target="{Binding ElementName=myLeftBorder}" Storyboard.TargetProperty="Width"
               To="0" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="OnUncheckedStoryboard">
            <DoubleAnimation Storyboard.Target="{Binding ElementName=myLeftBorder}" Storyboard.TargetProperty="Width"
              To="188" Duration="0:0:0.3"/>
        </Storyboard>
    </Window.Resources>
    <Window.Triggers>
        <EventTrigger RoutedEvent="ToggleButton.Checked" SourceName="myCheckBox">
            <EventTrigger.Actions>
                <BeginStoryboard Storyboard="{StaticResource OnCheckedStoryboard}">
                    <!--<Storyboard>
                        <DoubleAnimation Storyboard.Target="{Binding ElementName=myLeftBorder}" Storyboard.TargetProperty="Width"
                                     To="0" Duration="0:0:0.3"/>
                    </Storyboard>-->
                </BeginStoryboard>
            </EventTrigger.Actions>
        </EventTrigger>
        <EventTrigger RoutedEvent="ToggleButton.Unchecked" SourceName="myCheckBox">
            <EventTrigger.Actions>
                <BeginStoryboard Storyboard="{StaticResource OnUncheckedStoryboard}">
                    <!--<Storyboard>
                        <DoubleAnimation Storyboard.Target="{Binding ElementName=myLeftBorder}" Storyboard.TargetProperty="Width"
                                     To="188" Duration="0:0:0.3"/>
                    </Storyboard>-->
                </BeginStoryboard>
            </EventTrigger.Actions>
        </EventTrigger>
    </Window.Triggers>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Name="myLeftBorder" Width="188" Background="LightCyan">
            <TextBlock Text="菜单区域" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>
        <Border Grid.Column="1"  >
            <CheckBox Name="myCheckBox">
                <CheckBox.Style>
                    <Style TargetType="CheckBox">
                        <Setter Property="Width" Value="100"/>
                        <Setter Property="Background" Value="LightGreen"/>
                        <Setter Property="Height" Value="100"/>
                        <Setter Property="Content" Value="开"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="CheckBox">
                                    <Border Width="{TemplateBinding Width}"
                                            Height="{TemplateBinding Height}"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="{Binding RelativeSource={RelativeSource Mode=Self},Path=Width}">
                                        <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center" 
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="Red"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Content" Value="关"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="DarkGreen"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <EventTrigger.Actions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Width" Duration="0:0:0.3" To="120"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Height" Duration="0:0:0.3" To="120"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger.Actions>

                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <EventTrigger.Actions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Width" Duration="0:0:0.3" To="100"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Height" Duration="0:0:0.3" To="100"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger.Actions>
                            </EventTrigger>

                        </Style.Triggers>
                    </Style>
                </CheckBox.Style>
            </CheckBox>
        </Border>
    </Grid>
</Window>
