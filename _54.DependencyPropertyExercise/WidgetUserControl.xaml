<UserControl x:Class="_54.DependencyPropertyExercise.WidgetUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_54.DependencyPropertyExercise"
             mc:Ignorable="d"  Name="userControl"
             FontSize="30" Foreground="#666666" BorderBrush="#8cddcd" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <Binding RelativeSource="{RelativeSource Mode=Self}"/>
    </UserControl.DataContext>

    <Border BorderBrush="{Binding ElementName=userControl, Path=BorderBrush}">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}, Path=BorderBrush}"/>
                <Setter Property="BorderThickness" Value="0 3 0 0"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="Padding" Value="10"/>
                <Setter Property="Margin" Value="5"/>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Value }" 
                       Foreground="{Binding ElementName=userControl, Path=Foreground}"
                       FontSize="{Binding ElementName=userControl, Path=FontSize}"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding Title}" FontSize="16"
                       Foreground="{Binding ElementName=userControl, Path=Foreground}"
                       TextWrapping="Wrap"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" 
                       Foreground="{Binding ElementName=userControl, Path=BorderBrush}"
                       Text="{Binding Icon}" FontSize="40" VerticalAlignment="Center" />
        </Grid>
    </Border>


</UserControl>
