<Window x:Class="_55.TrayControlExercise.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_55.TrayControlExercise"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <local:TrayControl x:Name="trayControl" Grid.Row="0" 
                           Size="{Binding ElementName=sliderSize, Path=Value}" 
                           Count="{Binding ElementName=sliderCount, Path=Value}"/>
        <StackPanel Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Size "/>
                <Slider Name="sliderSize"  Minimum="1" Maximum="100" IsSnapToTickEnabled="True" Height="20" Width="300"/>
            </StackPanel>
            <Rectangle Height="20"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Count "/>
                <Slider Name="sliderCount" Minimum="1" Maximum="200" IsSnapToTickEnabled="True" Height="20" Width="300"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="选中数量"/>
                <TextBlock Text="{Binding ElementName=trayControl, Path=SelectedCount}"/>
            </StackPanel>
            <Button Content="Move" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
